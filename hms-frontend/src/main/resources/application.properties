# HMS Frontend Configuration
server.port=8081

# ----------------------------------------
# 📡 BACKEND CONNECTIVITY (DISTRIBUTED)
# ----------------------------------------
# Set BACKEND_IP to the machine running the Spring Boot API
hms.backend.ip=localhost
hms.backend.url=http://${hms.backend.ip}:8080

# Logging
logging.level.com.capgemini.hms=DEBUG
